<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/navigation_dashboard">

    <fragment
        android:id="@+id/navigation_dashboard"
        android:name="com.hamal.egg.ui.dashboard.DashboardFragment"
        android:label="@string/title_dashboard"
        tools:layout="@layout/camera_view"
        app:launchSingleTop="true"
        app:popUpTo="@id/navigation_dashboard"
        app:popUpToInclusive="true" />

    <fragment
        android:id="@+id/navigation_notifications"
        android:name="com.hamal.egg.ui.notifications.NotificationsFragment"
        android:label="@string/title_notifications"
        tools:layout="@layout/fragment_video"
        app:launchSingleTop="true"
        app:popUpTo="@id/navigation_notifications"
        app:popUpToInclusive="true" />

    <fragment
        android:id="@+id/navigation_settings"
        android:name="com.hamal.egg.SettingsFragment"
        android:label="@string/title_notifications"
        app:launchSingleTop="true"
        app:popUpTo="@id/navigation_settings"
        app:popUpToInclusive="true" />
</navigation>